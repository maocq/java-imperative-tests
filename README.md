# Java Imperative Tests

```shell
curl --location 'http://localhost:8080/api/usecase' \
--header 'Content-Type: application/json' \
--data '{
    "id" : 4000,
    "name" : "Foo",
    "statusId" : "id"
}'`
```